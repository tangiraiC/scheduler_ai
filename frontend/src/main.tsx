import React from "react";
import ReactDOM from "react-dom/client";

import SchedulerAIDashboard from "./SchedulerAIDashboard";
import "./styles.css";

ReactDOM.createRoot(document.getElementById("root")!).render(
  <React.StrictMode>
    <SchedulerAIDashboard />
  </React.StrictMode>,
);
